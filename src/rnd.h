/* The main header of the rnd library */
#ifndef RND_H
#define RND_H

#include "rnd_stack.h"
#include "rnd_queue.h"

int rnd_is_debug(void);
int rnd_is_quiet(void);
int rnd_is_abort(void);

#endif /* RND_H */
